# Simple Configuration
# Minimal setup for a single account

version: "1"
timezone: "UTC"

settings:
  ping_prompt: "ping"
  model: "claude-sonnet-4-20250514"
  dry_run: false

accounts:
  default:
    token_secret: "CLAUDE_CODE_OAUTH_TOKEN"
    enabled: true
